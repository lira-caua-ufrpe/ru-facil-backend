server:
  port: 8084

spring:
  application:
    name: SERVICO-ACESSO

eureka:
  client:
    service-url:
      defaultZone: http://service-discovery:8761/eureka
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
